1. Follow the package and class conventions as it in the project
2. If creating any new menu, please keep it in the ```explainfx/menu``` package
3. Any new Drawable object (shapes, arrows, image) must go in the ```explainfx/drawables``` package
4. Do not add any class outside of the subpackages (inside the ```explainfx``` package). Please create a new package
5. No AI slop/features
6. Do not use any AI generated images, icons, or assets.
7. Please avoid using Agentic IDE's or excessive use of LLM's
8. UI must be clean, minimal and simple. Do not clutter the UI
